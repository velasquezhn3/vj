# Bot VJ - Variables de Entorno PRODUCCIÓN
# ⚠️ CRÍTICO: Cambiar TODOS los valores por defecto antes de deploy

# Entorno de ejecución
NODE_ENV=production

# Configuración del Bot WhatsApp
GRUPO_JID=120363401911054356@g.us

# Configuración de Logs
LOG_DIR=./logs

# Base de Datos
DB_PATH=./bot_database.sqlite

# Puerto del servidor admin
ADMIN_PORT=4000

# Configuración de Seguridad - ⚠️ CAMBIAR OBLIGATORIAMENTE
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=GENERAR_NUEVO_JWT_SECRET_CON_CRYPTO_RANDOM_64_CHARS_ANTES_DE_PRODUCCION
ADMIN_DEFAULT_USERNAME=adminprod
ADMIN_DEFAULT_PASSWORD=CAMBIAR_PASSWORD_SEGURO_16_CHARS_MIN_Con_Simbolos!123

# Rate Limiting para producción (más restrictivo)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50
LOGIN_RATE_LIMIT_MAX=5

# Configuración de Limpieza Automática
CLEANUP_INTERVAL=30
CLEANUP_TIMEOUT=12

# Configuración de Backup Automático
BACKUP_ENABLED=true
BACKUP_INTERVAL_HOURS=4
BACKUP_RETENTION_DAYS=14
BACKUP_DIR=./backups
BACKUP_COMPRESSION=true
BACKUP_VERIFY=true

# Seguridad adicional
DISABLE_LOGGING=false
VERBOSE_LOGGING=false
DEBUG_MODE=false

# Headers de seguridad adicionales
TRUST_PROXY=true
SECURE_COOKIES=true
COOKIE_SAME_SITE=strict

# SSL/TLS (si aplica)
HTTPS_ENABLED=false
SSL_CERT_PATH=
SSL_KEY_PATH=
